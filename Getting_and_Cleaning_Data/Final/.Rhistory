library(xlsx)
library(xlsx)
setwd("C:/Users/Jacob/course/Coursera_Data_Science_Specialization-JHU/Getting_and_Cleaning_Data/Final")
source("run_analysis.R")
?group_by
library(dplyr)
?group_by
names(goodframe)[,1:2] <- c("Subject", "Activity")
names(goodframe)[1:2] <- c("Subject", "Activity")
gsub("\\-","",names(goodframe)[3:5])
gsub("\\-&\\(\\)","",names(goodframe)[3:5])
gsub("\\-","",names(goodframe)[3:5])
gsub("\\(\\)","",names(goodframe)[3:5])
names(goodframe) <- tolower(gsub("\\-","",names(goodframe)[3:5]))
source("run_analysis.R")
View(goodframe)
source("run_analysis.R")
head(arrange(goodframe,subject))
head(arrange(goodframe,subject,activity))
goodframe <- arrange(goodframe,subject,activity)
goodframe %>% group_by(subject, activity)
?ddply
library(dplyr)
?ddply
library(reshape2)
?ddply
ddply(goodframe,c("subject","activity"),summarize,mean=mean(names(goodframe[3:68]))
)
ddply(goodframe,.(subject, activity),summarize,mean=mean(names(goodframe[3:68])))
ddply
library(plyr)
library(plyr); library(dplyr)
ddply(goodframe,.(subject, activity),summarize,mean=mean(names(goodframe[3:68])))
warnings()
ddply(goodframe,.(subject, activity),summarize,names(goodframe[3:68])=mean(names(goodframe[3:68])))
ddply(goodframe,.(subject, activity),summarize,.(names(goodframe[3:68]))=mean(names(goodframe[3:68])))
ddply(goodframe,.(subject, activity),summarize,.(names(goodframe[3:68]))<-mean(names(goodframe[3:68])))
ddply(goodframe,.(subject, activity),summarize,.(names(goodframe[3:68]))<-mean(.(names(goodframe[3:68]))))
ddply(goodframe,.(subject, activity),summarize,.(names(goodframe[3:68]))=mean(.(names(goodframe[3:68]))))
xt <- xtabs(names(goodframe[3:68])~subject + activity, data = goodframe)
xt <- xtabs(tbodyacc_mean_x~subject + activity, data = goodframe)
head(xt)
ddply(goodframe,.(subject, activity),summarize,mean=mean(tbodyacc_mean_x))
summarize(goodframe)
ddply(goodframe,.(subject, activity),summarize, numcolwise(mean))
ddply(goodframe,.(subject),summarize, numcolwise(mean))
ddply(goodframe,.(subject),numcolwise(mean))
ddply(goodframe,.(subject, activity),numcolwise(mean))
tidy <- ddply(goodframe,.(subject, activity),numcolwise(mean))
View(tidy)
ftable(tidy)
xt <- xtabs(numcolwise(mean) ~ subject + activity, data = goodframe)
tidy <- ddply(goodframe,.(activity, subject),numcolwise(mean))
write.table(tidy, file="tidy_data.txt", row.names = FALSE)
tidy <- ddply(goodframe,.(subject, activity),numcolwise(mean))
write.table(tidy, file="tidy_data.txt", row.names = FALSE)
View(goodframe)
